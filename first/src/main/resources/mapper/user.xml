<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.first.vo.UserVO">
	<insert id="register">
		insert into user_tbl(user_id, user_pw, firstname, lastname) 
		values(#{user_id}, #{user_pw}, #{firstname}, #{lastname})
	</insert>
</mapper>