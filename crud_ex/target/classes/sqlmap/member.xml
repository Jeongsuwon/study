<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	
<!-- 회원가입 -->
	<insert id="join">
		insert into member(userid, userpw, nickname, social)
		values (#{userid}, #{userpw, jdbcType=VARCHAR}, #{nickname}, #{social, jdbcType=VARCHAR})
	</insert>
</mapper>